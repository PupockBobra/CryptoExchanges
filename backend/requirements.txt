fastapi==0.115.0
uvicorn[standard]==0.30.6
ccxt==4.5.51
redis[hiredis]==5.1.1
asyncpg==0.29.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2
curl_cffi==0.7.4
# 6.x runtime still accepts ccxt's MEXC 5.29.x gencode (one-major-back guarantee)
# and is required by finam-sdk stubs (gencode 6.33).
protobuf==6.33.5
aiohttp==3.13.5
# Official Finam Trade API gRPC SDK (import name: finam_trade_api) — tick-by-tick
# SubscribeOrderBook stream for the SPB order-book page.
finam-sdk==2.17.0
# Dev/test only — run with: docker compose exec backend python -m pytest
pytest==9.1.1

# Excel export of the MM-presence summary (GET /api/mmdetect/export.xlsx).
# Pure Python, no system deps — the CSV export stays for anything scripted.
openpyxl==3.1.5

# MTProto client for the SPB funding auto-ingest from @beststocks_neo.
# A *user* session is required: the Bot API cannot read a channel the bot isn't
# an admin of.  See backend/app/spb/funding_tg.py.
telethon==1.44.0
